{
  "private": true,
  "scripts": {
    "test": "pnpm --filter @pandotic/universal-cms test",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm --filter @universal-cms/admin-core build && pnpm --filter @universal-cms/admin-ui build && pnpm --filter @pandotic/universal-cms build && pnpm --filter @pandotic/skill-library build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "packageManager": "pnpm@9.15.0",
  "devDependencies": {
    "@changesets/changelog-github": "^0.6.0",
    "@changesets/cli": "^2.30.0",
    "supabase": "^2.0.0",
    "vitest": "^4.1.3"
  }
}
